{
  "name": "meta-cart-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Unified Cart Chrome extension — captures add-to-cart clicks and forwards them to the backend.",
  "scripts": {
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@eslint/js": "^9.9.0",
    "eslint": "^9.9.0",
    "eslint-config-prettier": "^9.1.0",
    "globals": "^15.9.0",
    "prettier": "^3.3.3"
  }
}
